{
  "id": "claude-tools",
  "version": "1.1.0",
  "name": "Claude Code + Bun + Python",
  "description": "Installs the native Claude Code binary, Bun, Python, and opus/fable/agents wrapper commands.",
  "documentationURL": "https://github.com/rynz/devcontainer-features/tree/main/src/claude-tools",
  "options": {
    "installBun": {
      "type": "boolean",
      "default": true,
      "description": "Install Bun to /usr/local/bin"
    },
    "claudeVersion": {
      "type": "string",
      "default": "latest",
      "description": "Claude Code version to install (latest, stable, or a specific version)"
    },
    "installWrappers": {
      "type": "boolean",
      "default": true,
      "description": "Install the opus, fable and agents wrapper commands"
    },
    "installPython": {
      "type": "boolean",
      "default": true,
      "description": "Install python3, pip, venv and the python alias via apt"
    }
  },
  "installsAfter": ["ghcr.io/devcontainers/features/common-utils"]
}
